---
"@neovici/cosmoz-omnitable-treenode-column": minor
---

Adopt cosmoz-tokens and Storybook system-design setup, render a disabled `cosmoz-input` header when filtering is disabled, use inline autocomplete styling, and retarget key dependencies to stable-compatible union ranges.
